1.Spriter 目前不支持层级切换，必须保持每个层级的元素存在，如果该层某元素不想显示，请把Alpha值设为0；
2.用TexturePacker.exe打包骨骼图片为纹理集时，不能在纹理集信息资源XML文件里设置scale属性，这跟UI的纹理集不一样；
3.保证循环的流畅，第一帧跟最后一帧保持一致；
4.为适配部分Android机型，打包纹理集时保证是正方形且宽高为2次幂；
5.不要设置Spriter软件右侧Object 的pivot x 和pivot y 的值，不然会导致动画位置错乱；
